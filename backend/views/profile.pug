extends layout.pug 

block content
  if building.length > 0
    h1 #{building[0].name}
    ul(style="padding:0;")
      each image in building
        li(style='list-style:none;display:inline;') 
          img(src=image.url, style='height:500px;')
  else 
    h1 Not found.