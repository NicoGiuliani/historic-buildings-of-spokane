style. 
  #mainNav {
    height: 12vh;
  }
  #mainFooter {
    height: 6vh;
  }
  @media screen and (max-width: 992px) {
    * {
      font-size: 1.4rem !important;
    }
    #mainNav {
      height: initial;
    }
    #mainFooter {
      height: 4vh;
    }
  }
  /* Hide the default scrollbar */
  ::-webkit-scrollbar {
    width: 12px;
  }
  /* Track (the area behind the thumb) */
  ::-webkit-scrollbar-track {
    background: transparent; /* You can set a background color here if desired */
  }
  /* Thumb (the draggable part) */
  ::-webkit-scrollbar-thumb {
    background: #888; /* Color of the thumb */
  }
  /* Handle the corner where two scrollbars meet */
  ::-webkit-scrollbar-corner {
    background: transparent;
  }
  /* Resize the content to accommodate the scrollbar width */
  body {
    margin-right: 12px; /* Width of the scrollbar */
    overflow-y: auto; /* Always show scrollbar */
  }

doctype html
html(lang="en")
  head
    title= pageTitle
    link(rel="stylesheet" href="/bootstrap.min.css")
    link(rel="stylesheet" href="/node_modules/@splidejs/splide/dist/css/splide.min.css")
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js')
    script(src="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/js/splide.min.js")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css")
    
  body.d-flex.flex-column(style="min-height: 100vh")
    nav#mainNav.navbar.navbar-expand-lg.bg-light(data-bs-theme='light').py-lg-0
      .container-fluid.px-4
        a.navbar-brand(href='/') Historic Buildings of Spokane
        button.navbar-toggler(type='button', data-bs-toggle='collapse', data-bs-target='#primaryNavMenu', aria-controls='primaryNavMenu', aria-expanded='false', aria-label='Toggle navigation')
          span.navbar-toggler-icon
        #primaryNavMenu.collapse.navbar-collapse.px-4.px-lg-0
          ul.navbar-nav.me-auto
            li.nav-item.my-3
              a.nav-link(href='/buildings') Buildings
          form.d-flex.my-auto(action='/search', method='get')
            input.form-control.me-sm-2(required, type='search', name='query', placeholder='Search')
            button.btn.btn-secondary.my-2.my-sm-0(type='submit') 
              i.fas.fa-search
              
    block content

    #mainFooter.d-flex.justify-content-center.align-items-center(style="margin-top: auto;")
      h5.m-0 Copyright © 2023 Nico Giuliani

    script(src="/node_modules/@splidejs/splide/dist/js/splide.min.js")
